export default function AdminDashboard() {
    return (
        <div className="space-y-3">
            <h1 className="retro-title">Dashboard</h1>
            <p className="text-sm text-[var(--subt)]">
                Use the menu on the left to create posts/galleries or manage existing content and tags.
            </p>
        </div>
    );
}
